# Example Secret. Prefer creating this out-of-band:
#   kubectl -n remote-tools create secret generic remote-tools-auth \
#     --from-literal=TS_AUTHKEY=tskey-auth-...
#
# Or copy k8s/secret.env.example → k8s/secret.env and use the install script.
# This placeholder is intentionally invalid so a forgotten apply cannot join
# a real tailnet; replace before production use.
apiVersion: v1
kind: Secret
metadata:
  name: remote-tools-auth
  namespace: remote-tools
  labels:
    app.kubernetes.io/name: remote-tools
    app.kubernetes.io/part-of: remote-tools
type: Opaque
stringData:
  TS_AUTHKEY: "tskey-auth-REPLACE-ME"
